---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart galaxy exporter timer
  ansible.builtin.systemd:
    name: galaxy-exporter.timer
    state: restarted
  become: true
