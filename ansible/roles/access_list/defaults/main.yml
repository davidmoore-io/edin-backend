---
# access_list role owns the on-host log directories under /var/log/edin/.
# Authorization is now managed via the Kaine AdminPage Commanders tab and
# Authentik group membership; this role no longer carries an FID allowlist.
# The role retains a small operational task surface for the log directory.

# Where the backend writes rejected login attempts as one JSON object per
# line. Lives under /var/log/ — the conventional Linux location for system
# and service logs, independent of any container rebuild. The access_list
# role ensures the directory exists on the host with the right perms, and
# the control_api docker-compose template bind-mounts it read-write into
# the container so writes land on the host filesystem, not in the container
# overlay (where they'd be lost on the next deploy).
commander_login_attempt_log: /var/log/edin/login-attempts.log

# Where the backend writes admin-action audit lines (Grant/Revoke/Link/Unlink
# /Approve/Deny mutations through the Kaine AdminPage Commanders tab). Same
# /var/log/edin/ directory + bind-mount as the denial log; same JSON-lines
# shape — a single canonical "who changed what access" record for incidents.
commander_admin_actions_log: /var/log/edin/admin-actions.log
