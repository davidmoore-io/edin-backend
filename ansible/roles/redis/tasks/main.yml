---
# Redis session store for EDIN control API

- name: Ensure Redis compose directory exists
  ansible.builtin.file:
    path: "{{ redis_compose_path | dirname }}"
    state: directory
    owner: root
    group: root
    mode: '0755'
  tags: [redis]

- name: Ensure Redis data directory exists
  ansible.builtin.file:
    path: "{{ redis_data_dir }}"
    state: directory
    owner: "999"
    group: "999"
    mode: '0755'
  tags: [redis]

- name: Create edin-app Docker network
  community.docker.docker_network:
    name: "{{ edin_app_network }}"
    state: present
  tags: [redis, network]

- name: Template Redis Docker Compose
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: "{{ redis_compose_path }}"
    owner: root
    group: root
    mode: '0644'
  notify: Restart redis
  tags: [redis, compose]

- name: Start Redis
  ansible.builtin.command:
    cmd: docker compose -f {{ redis_compose_path }} up -d redis
  register: redis_up
  changed_when: "'Started' in redis_up.stderr or 'Created' in redis_up.stderr"
  tags: [redis]

- name: Wait for Redis to be healthy
  ansible.builtin.command:
    cmd: docker compose -f {{ redis_compose_path }} ps --format json
  register: redis_ps
  retries: 5
  delay: 3
  until: redis_ps.rc == 0
  changed_when: false
  tags: [redis, verify]
