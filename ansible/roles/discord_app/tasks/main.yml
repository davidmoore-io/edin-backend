---
# Discord App — sync application state (commands, roles)
# Runs locally (delegate_to: localhost), no server deployment
# TODO: implement when Discord app registration sync is needed
- name: Discord app sync placeholder
  ansible.builtin.debug:
    msg: "Discord app sync not yet implemented for edin-space. Use 'make discord-sync' from edin-backend."
  delegate_to: localhost
  become: false
  tags: [discord_app]
