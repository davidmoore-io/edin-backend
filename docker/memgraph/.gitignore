# init.cypher is generated by render-init.sh from the edin-data Jinja template.
# Keeping it gitignored ensures the source of truth stays in edin-data and we
# never edit the rendered output by mistake.
init.cypher
